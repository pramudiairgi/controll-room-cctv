[phases.setup]
nixPkgs = ["...", "php83", "php83Extensions.pgsql", "php83Extensions.pdo_pgsql"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"

[variables]
NIXPACKS_PHP_ROOT_DIR = "/app/public"
