[phases.setup]
nixPkgs = ["..."]

[phases.build]
cmds = ["rm -rf node_modules", "npm ci", "npm run build"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"

[variables]
NIXPACKS_PHP_ROOT_DIR = "/app/public"
